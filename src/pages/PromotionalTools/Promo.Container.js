import connect from 'redux-thunk';
import PrmoTools from './Promo.Component';

const mapStates = state => ({});
const mapDispatch = dispatch => {
  return {};
};

export default connect(
  mapStates,
  mapDispatch,
)(PrmoTools);